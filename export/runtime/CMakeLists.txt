cmake_minimum_required(VERSION 2.8.9)
set(CMAKE_C_STANDARD 99)
include_directories(include)
if(${MKLDNN})
    message("MKLDNN")
    add_definitions(-DMKLDNN)
    set(MKLDNN OFF CACHE BOOL OFF FORCE)
endif(${MKLDNN})
add_subdirectory(example)
